{
  "scripts": {
    "test": "firebase emulators:exec 'mocha --exit firestore.spec.js'"
  },
  "engines": {
    "node": ">= 18"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.0",
    "firebase": "10.1.0",
    "mocha": "10.2.0"
  }
}
