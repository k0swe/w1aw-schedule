{
  "scripts": {
    "test": "firebase emulators:exec --only firestore 'mocha --exit firestore.spec.js'"
  },
  "engines": {
    "node": ">= 18"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.0",
    "firebase": "10.3.1",
    "mocha": "10.2.0"
  }
}
